#include <stdio.h>
#include "main.h"

/**
 * mul - multiplication operation between two integers.
 * @a: integer number.
 * @b: integer number.
(*
 * Multiplication operation between two integers.
 * Return: mul.
 */

int mul(int a, int b)
{
	int mul;

	mul = (a * b);

	return (mul);
}
