#include "0-main.h"
#include <stdio.h>

/**
 * swap_int - swaps two values of integers.
 * @a: a* value int to point to a.
 * @b: b* value int to point to b.
 */
void swap_int(int *a, int *b)
{
	*a = *b;
}
