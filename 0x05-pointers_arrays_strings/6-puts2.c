#include "0-main.h"

/**
 * puts2 - prints other characters (pais)
 * @str: chain to traverse
 *
 */
void puts2(char *str)
{
	int i = 0;

	for (i = 0; str[x] != '\0'; x++)
	{
		if (i % 2 == 0)
			_putchar(str[i]);
	}
	_putchar('\n');
}
}
